# OptimizingShapePacking 

My IPYNBs to find the shortest known radius (r) / side length (s) of a container shape fitting n unit shapes inside of itself.

Current findings:

- **shortest known side length (s) of an equilateral triangle packed with n unit octagons for n = 26 to n = 50**

  - room for further optimization by raising search_sec and hop_sec values in cell 6 of octagons_in_triangles.ipynb
